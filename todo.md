todos before impl Candidate
---
1. The program should allow to start n number of nodes for a cluster, otherwise
 it defaults to two nodes that make a cluster
2. Each node should have at least 2 peers

